WHERE I LEFT OFF:

Making Field Drag and Drop not merge fields together

!--------!
   BUGS
!--------!

IMPORTANT BUGS:

-Users can access all resumes by typing in the URL
   -Need to implement auth requirements to allow proper user access/verification

BIGGER BUGS:

-Field Reordering Not persisting

-Adding new sections, subsections, etc causes prior styling changes to be undone

-Not all changes get saved on first try.
   -Ex:  I created new fields in the "skills" section, saved the resume, refreshed only to find roughly half of them remaining.
      -Tried again and it saved properly.
      -No idea why


MEDIUM BUGS:

-Dragging Fields is Clunky/Buggy

-Margins can go negative without any real, convenient way for the user to know this.

-Top margin indicator (left hand side ruler) doesn't work unless the user is actively clicked into a particular section

-Having three or more columns messes with the margin ruler indicators


SMALL BUGS:

-Drag Reordering of Fields doesn't show the visual cue when dragging over a row with "startNewRow" set to "true"
   -Still reorders, but doesn't look like it will

-Field drag destination state may be stale or persist between drags

-Border Inputs for left and right sometimes use wrong value

-Bullets don't allow for copy/pasting without breaking up indentation (shift + tab fixes indentation though, but is annoying)

-Margin Ruler Indicators should be touched up to more clearly depict their effects
   -Something like a tiny indicator to show where the section/column starts, and the main indicator to show where it ends up


TINY BUGS:

-Text Alignment on Placeholder Text becomes odd when aligned center or right


!------------!
   FEATURES
!------------!

STRETCH FEATURES:

-Resume Templates Sharing Publicly

MAIN FEATURES:

-Multi Page Resume

-Autosave Functionality

-Resume Templates
   -Ideally even have it where you can plug in default profile(s) info to auto fill resumes templates

-Demo Editor
   -For non logged in users to be able to try/test out resume editor
   -Initial functionality implemented
      -Need to insert a new test resume with ID 0 to deployed PostgreSQL DB
   

BIGGER FEATURES:

-Undo/Redo should stack across multiple fields/sections/subsections/etc.
   -Need to create some sort of call stack that can be traced backwards/forwards strikeThrough 
      -With some cut off probably, baseline of 10?  Since last save?  TBD.

-Reset of Styling, Layout, etc Accross the Board
   -Ex: Reset individualized margin/padding to defaults
   -Useful for cases of when small changes add up and become unmanageable
   -Likely a dropdown with checkboxes for font size, margin/padding, etc etc
   -Potentially show which sections, fields, etc are NOT default to even more clearly allow resets

-Column Setup/Organization
   -Need to add a way to have multiple columns on page, but also allowing particular sections to span multiple columns

-Adding Margin Bottom to Section Headings, Subsections, Fields, etc
   -Need to be able to streamline it as well so keep the resume looking uniform even with small changes

MEDIUM FEATURES:

-Improved Keyboard Shortcuts
   -Ease of life, the more intuitive the better
   -Also App-Specific Hotkeys like ctrl + enter while in a field automatically adding a new field to that subsection


SMALL FEATURES:

-Borders Customization
   -Moving them in/out/left/right/up/down/etc

-Allow "Apply To All" tag for Subsection Grid Settings, so changes to one subsection's layout can be mimicked automatically to others in the section
   -Will likely need to have a check for when new subsections are created afterwards

-Adding an "active" indicator for toolbar settings already in use
   -Ex: When the active selection is already bolded, the toolbar should indicate that, such as slightly highlighting the Bold button

TINY FEATURES:

-Allowing the adjustment of grid column widths within subsections
   -Gap too probably


MAYBES:

-Indentation

-Font Family
   -Allowing different font families to be applied to different sections, subsections, fields, and slate leaves
