!--------!
   BUGS
!--------!

MEDIUM BUGS:

-Dragging Fields is Clunky/Buggy
   -Particularly when dragging within grid layouts

-Undo/Redo functionality is buggy when adding/deleting primary content (fields, subsections, sections, and columns)
   
SMALL BUGS:

-Indenting bullets can only go to a second level
   -Should continue, at least 3 levels deep

-Border Inputs for left and right sometimes use wrong value
   -It may be defaulting to a predetermined value, unsure of what the issue really is specifically

TINY BUGS:

-Resume Slice reducers need some cleaning up
   -Update Subsection uses "subsectionId", while section/field/column use "id" for simplicity sake
   -Updating fields is split into several reducers
   -Column/Section/Subsection/Field updates could be much more uniform

OTHER BUGS:

-Session Cookie storage in incognito sites doesn't works 
   -Considered "Third Party" since the front end is making requests to a different URL with the API, needs configuration

!------------!
   FEATURES
!------------!

STRETCH FEATURES:

-Resume Templates Sharing Publicly

MAIN FEATURES:

-Multi Page Resume

-Autosave Functionality

-Resume Templates
   -Ideally even have it where you can plug in default profile(s) info to auto fill resumes templates

-Demo Editor Tutorial/Walk Through


MEDIUM FEATURES:

-Subsection and Field Gap/Spacing works resume wide, but not for individual parts of resume
   -Would be nice to have a drag feature for these too, or maybe margin indicator.
      -Need to be careful not to make it too crowded though

-Column Setup/Organization
   -Need to add a way to have multiple columns on page, but also allowing particular sections to span multiple columns

-Improved Keyboard Shortcuts
   -Ease of life, the more intuitive the better
   -Also App-Specific Hotkeys like ctrl + enter while in a field automatically adding a new field to that subsection

-Indentation
   -For things like bullets

SMALL FEATURES:

-Borders Customization
   -Moving them in/out/left/right/up/down/etc

-Allow "Apply To All" tag for Subsection Grid Settings, so changes to one subsection's layout can be mimicked automatically to others in the section
   -Will likely need to have a check for when new subsections are created afterwards

TINY FEATURES:

-Allowing the adjustment of grid column widths within subsections
   -Gap too probably


MAYBES:


-Font Family
   -Allowing different font families to be applied to different sections, subsections, fields, and slate leaves
