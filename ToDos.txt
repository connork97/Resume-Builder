WHERE I LEFT OFF:

I was trying to get the drag and drop for sections working.  I think the only code I added was in Section.jsx, with the drag handler functions, as well as extra data sets being added.

!--------!
   BUGS
!--------!

IMPORTANT BUGS:

-Users can access all resumes by typing in the URL
   -Need to implement auth requirements to allow proper user access/verification

BIGGER BUGS:

-Several styling changes to individual words/strings results in line break
   -Likely due to a new Slate Field/Leaf being created
   -For Line Height, potentially reflecting the new line height on all other strings in the field could fix it, TBD

-Section and Column Gaps don't work on individual section/columns
   -Only works resume wide

-ActiveSectionId should get put into ActiveSectionIds array
   -Once fully implemented, there will be less/no need for the singular ActiveSectionId

-Not all changes get saved on first try.
   -Ex:  I created new fields in the "skills" section, saved the resume, refreshed only to find roughly half of them remaining.
      -Tried again and it saved properly.
      -No idea why


MEDIUM BUGS:

-Can't properly adjust section padding
   -Specifically, when all sections have padding added globally from the toolbar, you can't reduce it on one specifically
      -Due to it registering as a negative value
         -Even though it is really a positive value when combined with the global padding


SMALL BUGS:

-Text Alignment on Placeholder Text becomes odd when aligned center or right

-Resume Gap and Column Padding can be Negative values and markers can go outside the ruler bounds
   -Should combine both values to ensure the total remains 0 or greater

-Having three or more columns messes with the margin ruler indicators

-Dropdowns don't lose focus/disappear when clicked out of
   -Sometimes intentional, typically not

-Autofilling the link input from preexisting links takes two tries to work initially after page load
   -Then works fine.  Likely some issue regarding page mount timeline

-Text align dropdown icon reflects the resume, section, subsection (I think), and simple fields.  But it does NOT reflect fields with multiple children (i.e. unordered lists with multiple bullets)

-Border Inputs for left and right sometimes use wrong value

-When there are multiple font sizes in one field (including multi line lists), the Font Size does not properly adjust

-Currently Editing Button often shows "undefined"

-Bullets don't allow for copy/pasting without breaking up indentation (shift + tab fixes indentation though, but is annoying)


TINY BUGS:

-Margin Rulers are every so slightly off target

-"School" name in education doesn't show up in outline
   -IF there is an empty line to start the field (from pressing enter before typing)


!------------!
   FEATURES
!------------!

STRETCH FEATURES:

-Resume Templates Sharing Publicly

MAIN FEATURES:

-Undo/Redo

-Multi Page Resume

-Autosave Functionality

-Resume Templates
   -Ideally even have it where you can plug in default profile(s) info to auto fill resumes templates

-Demo Editor
   -For non logged in users to be able to try/test out resume editor
   -Initial functionality implemented
   

BIGGER FEATURES:

-Reset of Styling, Layout, etc Accross the Board
   -Ex: Reset individualized margin/padding to defaults
   -Useful for cases of when small changes add up and become unmanageable
   -Likely a dropdown with checkboxes for font size, margin/padding, etc etc
   -Potentially show which sections, fields, etc are NOT default to even more clearly allow resets

-Drag and Drop Reorganization
   -First, for sections
      -Swap column and position values
   -Second, for subsections
      -Swap position values
   -Third, for fields
      -Swap position values
      -Potentially need to swap other styling values as well

-Outline "Start New Row" feature needs to be reworked.
   -It's quite annoying to use currently.


MEDIUM FEATURES:

-Improved Keyboard Shortcuts
   -Ease of life, the more intuitive the better
   -Also App-Specific Hotkeys like ctrl + enter while in a field automatically adding a new field to that subsection

SMALL FEATURES:

-Adding an "active" indicator for toolbar settings already in use
   -Ex: When the active selection is already bolded, the toolbar should indicate that, such as slightly highlighting the Bold button

-Borders Customization
   -Moving them in/out/left/right/up/down/etc

-Adding Margin Bottom to Section Headings


MAYBES:

-Indentation

-Font Family
   -Allowing different font families to be applied to different sections, subsections, fields, and slate leaves